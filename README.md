# 研数 · 考研数学 AI 自学系统

根据《考研数学AI自学系统开发文档 v1.0》实现的**单人本地 MVP**：每天和 AI 老师聊着学完知识点，智能出题验收，答错的题自动进错题本并按 SM-2 间隔重复调度复习，每日任务 + 专注计时自动打卡。

技术形态：**纯前端多文件应用**（原生 JS + CSS，零构建、零依赖、零后端）。

---

## 快速开始

**方式一：直接双击**（推荐，完全离线可用）

打开 `index.html`，浏览器以 `file://` 协议运行，所有数据存本机浏览器 localStorage。

> 数学公式默认走 KaTeX CDN，无外网时自动降级为高亮原文显示，不影响功能。
>
> **默认接云端 API（DeepSeek）**，`file://` 下也能用——去设置页填一个 Key 就行。
> 想改用本地大模型（LM Studio / Ollama）请用方式二，`file://` 协议下浏览器会拦截对 `localhost` 的请求。

**方式二：本地服务器**

```bash
cd kaoyan-math-tutor
python3 -m http.server 8080
# 浏览器打开 http://localhost:8080
```

**方式三：GitHub Pages 在线部署**

本仓库可直接推送至 GitHub 并开启 GitHub Pages（Settings → Pages → Branch `main` / folder `root`），即可获得免费在线访问地址，手机/平板无需安装即用。

---

## 功能清单

| 模块 | 说明 |
| --- | --- |
| 仪表盘 | 今日任务单（到期复习 / 新知识点 / 每日一练）、四项统计卡、专注计时器、26 周学习热力图、连续打卡天数 |
| 知识树 | 高数 7 章 + 线代 6 章 + 概率统计 6 章共 68 个考点，按考试范围（数一/二/三）过滤，节点级掌握状态（新/学习中/已掌握） |
| 聊天学习 | **AI Agent 内核**：模型带着 9 个工具自主作答——翻错题本、按水平抽题、读知识树、画函数图像、记笔记、标掌握。四种老师人设可切换；未接模型时自动降级为内置引擎 |
| 讨论模式 | 一个老师 + 三个水平不同的学生，围绕一道你错过的题掰扯一轮，最后老师点名点评谁对谁错；你可以随时插话打断 |
| 每日一练 | 每天 5 题，抽题策略优先"掌握度低 + 错题多的知识点"；选择/填空即时判分；每题带来源徽章（经典例题/真题改编/模拟题 + 年份） |
| 筛选练习 | 设置页按来源 / 年份 / 难度筛选题库（204 题全量），自定义单轮题量随机抽取，可随时"重新筛选" |
| 复习队列 | SM-2 四档自评（忘记/困难/记得/轻松），错题复习卡优先；到期自动出现在今日任务 |
| 错题本 | 答错自动收录，按知识点筛选，一键重练，做对后自动移出 |
| 统计 | 热力图、14 天正确率折线、三科掌握度雷达、各章正确率条形 |
| 设置 | 考试范围、每日新学量、目标考试日期、LLM 接入（Key 仅存内存）、数据重置 |

## 打卡规则

满足其一即视为当日打卡成功（进入连续天数与热力图统计）：

- 今日任务单 3 类任务全部完成（复习卡全评 + 新知识点全验收 + 每日一练全作答）
- 专注计时累计 ≥ 30 分钟

## 目录结构

```
kaoyan-math-tutor/
├── index.html              应用骨架与页面路由容器
├── css/style.css           设计系统（深蓝学术侧边栏、卡片、图表、答题卡、复习卡）
└── js/
    ├── sm2.js              SM-2 间隔重复调度器（四档自评 → quality 映射、EF 维护、到期日计算）
    ├── data-knowledge.js   知识树数据：3 科 19 章 68 节点（LaTeX 正文/难度/考试范围/关联）
    ├── data-questions.js   题库：204 道选择 + 填空（覆盖 68 节点、每节点 ≥2 题），逐题答案/解析/来源标注（经典例题/真题改编/模拟题 + 年份）
    ├── engine.js           内置教学引擎：名师状态机（讲解→练习→验收）+ 判题器（LLM 兜底通道）
    ├── llm.js              模型通道：本地服务探测（LM Studio/Ollama/vLLM）+ 流式对话 + 工具调用双通道
    ├── tools.js            Agent 工具集：9 个工具 + 安全表达式求值器（无 eval）+ 纯 SVG 函数绘图
    ├── agent.js            Agent 内核：agent loop、实时学情注入、老师人设、长对话压缩
    ├── discuss.js          讨论模式：选题、角色差异化、讨论生成、老师点评
    └── app.js              状态存储、每日任务引擎、Hash 路由与 8 个页面渲染、图表、打卡与计时器
```

## 数据模型（localStorage key: `kaoyan_math_tutor_v1`）

```jsonc
{
  "settings": {
    "examTrack": "math1", "dailyNew": 2, "examDate": "", "persona": "strict",
    "llm": {
      "enabled": true, "kind": "cloud",              // 通道：cloud | local
      "base": "...", "model": "...",                 // 当前生效（由 kind 决定取哪一组）
      "localBase": "http://127.0.0.1:1234/v1", "localModel": "", "localName": "",
      "cloudBase": "https://api.deepseek.com", "cloudModel": "deepseek-chat",
      "cloudKey": "", "rememberKey": false           // cloudKey 仅在勾选记住时才写入
    }
  },
  "cards":        { "<cardId>": { type: knowledge|mistake, knowledgeId, questionId?, due, interval, reps, ef, lapses } },
  "attempts":     [ { qid, kid, answer, correct, date } ],
  "checkins":     { "YYYY-MM-DD": { minutes, tasksDone } },
  "daily":        { date, reviewIds[], newIds[], quizIds[], reviewDoneIds[], newDoneIds[], quizDoneIds[] },
  "chats":        { "<kid>": { stage, history[], acceptQueue[], ... } },
  "notes":        { "<kid>": [ { text, date } ] },
  "discussions":  { "<kid>": { topic, messages[], summary, date } },
  "customQ":      [ { ...自建题 } ]
}
```

> 老版本数据在 `load()` 里自动补齐字段，并有一次性的 `defaultsV2` 迁移标记：只有**从未真正配置过模型**（既没本地模型名、也没记住云端 Key）的用户才会被切到云端默认，已配好的配置不会被冲掉。

## AI 老师：Agent 内核

聊天页的 AI 不是"关键词匹配 + 写死模板"，而是一个带工具的 agent loop。

**它能看到什么**：每次对话前，系统从 localStorage 实时算出你的学情并注入提示词——已掌握/学习中/未开始考点数、连续打卡天数、近 7 天正确率、最薄弱的几个考点及错误次数、当前考点的作答历史与待攻克错题。模型据此决定讲什么、讲多深。

**它能做什么**（9 个工具，全部读写你的真实数据）：

| 工具 | 作用 |
| --- | --- |
| `query_weakness` | 查最薄弱的考点排行 |
| `get_mistakes` | 翻错题本（答错且尚未做对的题） |
| `pick_question` | 按知识点 + 难度加权抽题 |
| `get_node` | 读某个考点的完整正文与关联考点 |
| `search_nodes` | 按关键词检索知识树 |
| `get_progress` | 读整体进度、打卡、距考试天数 |
| `draw_graph` | **把函数画成图像直接显示**（讲极限/单调性/凹凸性/面积时） |
| `save_note` | 把总结记到考点下 |
| `mark_mastered` | 标记已掌握并生成复习卡片 |

`draw_graph` 配了一个自写的递归下降表达式求值器（**不使用 eval**），支持 `+ - * / ^`、括号、隐式乘法（`2x`、`3sin(x)`）、`sin/cos/tan/ln/log/exp/sqrt/abs` 等函数与 `pi`、`e` 常数，输出纯 SVG 曲线，无图表库依赖。

**四种老师人设**（设置页切换，同一内核的不同提示词策略）：严格督学 / 段子手 / 苏格拉底 / 应试机器。

**模型通道**（设置页分段切换，默认**云端**）：

- **云端 API（默认）**：开箱预填 DeepSeek（`https://api.deepseek.com` + `deepseek-chat`），也兼容任何 OpenAI 协议服务（Kimi / 通义 / 自建网关）。**去设置页填一个 Key 即可开聊**，无需任何本地环境。
  Key 默认只存内存，刷新页面需重填；想省事可勾「记住 Key」（写入本机 localStorage，**明文**——只在自己电脑上勾）。
- **本地模型**：设置页点"探测本机"自动扫描 LM Studio（1234）、Ollama（11434）、vLLM（8000）、llama.cpp（8081）。免 Key、离线、数据不出本机。

> 两条通道的配置**各存一份、互不覆盖**：切到云端不会清掉你的本地模型名，切回本地也不会丢掉云端 Base。

**两条健壮性保障**：

1. 模型不支持原生 `tools` 参数（本地小模型常见）时，自动降级为**文本工具协议**（` ```tool ` 代码块），不必换模型。
2. 模型完全不可用时，静默降级回内置教学引擎，聊天不中断。

> **注意**：默认的云端通道在 `file://` 下可直接用。只有改用**本地模型**时才需要方式二——`file://` 协议会拦截对 `localhost` 的请求，请在该目录执行 `python3 -m http.server 8080` 后访问 `http://localhost:8080`；LM Studio 需在 Server 设置中开启 CORS。

### 「测试连接」失败怎么排查

点「测试连接」会先问 `/models` 拿模型列表；若该地址没有这个接口，会自动改发一次**最小对话请求**来判定——因为真正要回答的问题是「能不能对话」，而不是「有没有 `/models`」。

失败提示会**按原因分开**，别混着看：

| 提示 | 含义 | 怎么办 |
| --- | --- | --- |
| `已连通，但鉴权没过` | 网络通了，Key 被拒（401/403） | 检查 Key 是否复制完整、有无多余空格、是否欠费 |
| `连不上` | 压根没建立连接 | 查网络 / 代理 / 防火墙；确认接口地址没填错 |
| `连接超时` | 建立了连接但没响应 | 云端查网络；本地看模型是否还在加载 |
| `返回的不是 OpenAI 兼容接口` | 连上了但响应不是 JSON | 接口地址应填**根地址**（如 `https://api.deepseek.com`），不要带 `/chat/completions` |
| `接口能连上，但模型列表里没有「X」` | 连得上，模型名写错了 | 按提示里列出的可用模型名改 |

> 提示语本身也**认通道**：云端地址不会让你去「启动本地推理服务」，本机地址也不会让你去「检查 API Key 余额」。

## 讨论模式：老师 + 三个学生

在知识点页点「开一局讨论」，换一种学法。三个学生水平不同，围绕一道**你做错过的题**掰扯一轮，最后老师点名点评。

**角色不是靠提示词里的形容词区分的**——那样模型会趋同，三个人说出同一种 AI 腔。差异化靠三样东西：

| 角色 | 掌握的信息 | 典型表现 |
| --- | --- | --- |
| 学生甲（思路快） | 完整正文 + 例题 + 关联考点 | 一针见血，爱把规律总结成一句话 |
| 学生乙（中等） | 正文 + 例题 | 跟得上，但爱追问"为什么" |
| 学生丙（基础薄弱） | **只有第一句定义** | 口语化、用错术语、想当然 |

信息量递减 → 必然产生分歧。更关键的是：**丙犯的错从你的错题本和题目干扰项里挖**，不是模型随机编的。所以他踩的坑，就是你真的会踩的坑。

**一轮流程**：老师抛题 → 甲发言 → 乙追问 → 丙暴露误解 → 甲纠正丙 → 老师点名点评（谁对、谁错在哪一步、这个考点必记的 2-3 条结论）。

**你可以随时插话**，老师会停下来直接回应你，再继续讨论。

**生成成本**：老师那两段是真调用（吃你的真实学情），三个学生的发言一次生成。整场 **2 次模型调用**——云端约 5-15 秒，本地模型视机器而定，用逐字打字掩盖等待。

**选题优先级**：错过次数最多的题 → 正确率最低的考点里抽一题 → 当前考点随机一题。

## 判题容错

选择题精确匹配；填空题支持：全半角与全角标点归一、`1/2` 与 `0.5` 等价、π/pi/`\pi` 互认、`x^2` 与 `x²` 归一、数值 1e-6 容差。

## 已知限制（MVP 边界）

- 无账号体系，数据跟随浏览器（换浏览器/清缓存即重置，可在设置页导出数据前先备份思路）
- 内置题库 204 题（覆盖 68 个考点、每节点 ≥2 题），含来源标注（经典例题/真题改编/模拟题 + 年份）；可在设置页按来源/年份/难度筛选练习
- 验收从当前考点加权抽 3 题（真题改编优先、难度贴近考点难度）
- 未实现 V2.0 路线图中的多设备同步、语音交互、AI 生成个性化题目（详见开发文档路线图）

## 开发文档对照

- 复习调度：开发文档 8.4.3 的降级方案即 SM-2，本实现含 EF 衰减与四档自评
- 聊天学习：文档 5.3/6 的教学流程（讲解→追问→验收）
- 在线出题判题：文档 8.1.1 选择题 + 8.1.2 填空题 + 归一化判题
- 每日打卡统计：文档 5.1 仪表盘与 8.6 统计
- 题库来源标注：文档 7.2 出题引擎——字段 `sourceType` / `sourceYear` / `source`

## 技术栈

- 前端：原生 JavaScript（ES5 兼容）+ 原生 CSS
- 数学公式：KaTeX（CDN 加载，离线自动降级）
- 图表：纯 SVG + CSS 动画，无图表库依赖
- 数据存储：localStorage（浏览器本地持久化）
- 零构建、零 npm 依赖，任何浏览器打开 `index.html` 即可运行

## License

MIT License — 详见 [LICENSE](LICENSE)
```